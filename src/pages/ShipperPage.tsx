// صفحة مخصصة للتاجر تعرض فقط:
// - شحناته الخاصة
// - إمكانية إضافة شحنة جديدة
// - تتبع الشحنات
// - تقارير مالية خاصة به
// بدون أي إمكانية رؤية المناديب أو المستخدمين الآخرين

// مثال على هيكل الصفحة:
export default function ShipperPage() {
  // جلب الشحنات المرتبطة بـ shipper_id الخاص بالمستخدم الحالي فقط
  // عرض لوحة معلومات مبسطة
  // أزرار محدودة (إضافة شحنة، تتبع، تقارير)
  
  return (
    <div className="container py-6 space-y-6" dir="rtl">
      <h1 className="text-2xl font-bold">لوحة تحكم التاجر</h1>
      <p className="text-muted-foreground">مرحباً بك في لوحة تحكمك. يمكنك إدارة شحناتك الخاصة هنا.</p>
      {/* محتوى مخصص للتاجر فقط */}
    </div>
  );
}